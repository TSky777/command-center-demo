{
  "name": "command-center-demo",
  "version": "1.0.0",
  "description": "Command Center — analytics dashboard demo (Home + Expenses). Seeded sample data, no external services required.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"node --watch server/index.js\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "build:pages": "node tools/gen-static-data.js && cd client && npm install && VITE_DEMO_STATIC=1 vite build",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.2",
    "@google/generative-ai": "^0.24.1",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.10.0",
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "groq-sdk": "^1.2.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
